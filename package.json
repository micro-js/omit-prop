{
  "name": "@f/omit-prop",
  "description": "Non-destructively omit a nested property",
  "repository": "git://github.com/micro-js/omit-prop.git",
  "version": "1.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/omit": "^2.0.0",
    "@f/set-prop": "^0.1.0"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
