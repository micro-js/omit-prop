{
  "name": "@micro-js/omit-prop",
  "description": "Non-destructively omit a nested property",
  "repository": "git://github.com/micro-js/omit-prop.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@micro-js/omit": "^2.0.0",
    "@micro-js/set-prop": "^0.1.0"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "standard": "^5.4.1"
  }
}
